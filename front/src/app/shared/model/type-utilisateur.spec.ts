import { TypeUtilisateur } from '../service/type-utilisateur';

describe('TypeUtilisateur', () => {
  it('should create an instance', () => {
    expect(new TypeUtilisateur()).toBeTruthy();
  });
});
